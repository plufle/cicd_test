[pytest]
addopts = -v -p no:randomly --tracing retain-on-failure --screenshot only-on-failure --video retain-on-failure --alluredir=reports
testpaths = tests
markers =
    ui: UI tests that require a browser
    api: API tests that use the authenticated request context
    smoke: quick sanity tests for CI gates
